#!/bin/bash
: ${XRAYLIB_DIR:=$HOME/.xraylib}
TMPDIR=$XRAYLIB_DIR/bin
if [ -d $TMPDIR ]; then
    STOREDIR=$(pwd)
    cd $TMPDIR
    python runxraylib.py "$*"
    cd $STOREDIR
else
    echo "Directory $TMPDIR does not exist"
    exit 1
fi
